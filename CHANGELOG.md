# 更新记录

本项目仍处于早期开发阶段。版本标签用于标记可复现的开发进度，不表示已经提供签名、公证、
安装包或正式发售支持。

## 1.0.3 — 2026-08-24

- 用量页改为统一时间范围查询，支持年、月、周、天四档切换，并按客户端、模型、后端和状态筛选。
- Token 趋势明确拆分输入（缓存命中）、输入（缓存未命中）与输出，保留后端精确 Usage 计量口径。
- 新增全年 365 天请求活动图；日期选择可联动单日数据，再次点击可取消并恢复此前范围。
- 范围切换保留上一帧数据，消除日期联动时的整页加载闪动；未来日期不再显示为零用量。
- 用量构成、主要客户端、缓存命中率、计量覆盖率和请求明细共用同一范围与筛选语义。
- 操作记录界面收敛为最近 50 条，历史记录仍按本机保留策略存储。

## 1.0.2 — 2026-08-24

- Agent RPC 从 v5 演进到 v9，新增四类外部 Agent 的通用受控配置、脱敏运维历史、部署就绪短时观测、Pi 私有安装与可恢复卸载计划。
- Pi 私有部署固定官方包版本、Registry、顶层 SRI 和完整 lockfile v3 依赖闭包；用户安装、HAL100 私有运行时和内置 Pi Agent Core 保持独立。
- 一级导航收敛为首页、模型与运行、软件接入、Agent、活动；设置独立位于侧栏底部，旧路由继续确定性重定向。
- 模型、运行、推理服务、软件接入、Agent、用量、操作记录和设置按任务重构，并采用页面级代码分割。
- Drawer 改为完整 WebView Portal，补齐焦点循环、滚动锁、主应用 `inert` 和不透明背景；启动失败和 React 渲染异常提供可操作恢复页，不再退化为白屏。
- 完成 880×620 与 1280×800 页面回归、真实 Tauri 冷启动验收以及全仓静态检查、测试和生产构建。

## 1.0.1 — 2026-08-20

- 完成模块化 HAL100 Agent 能力架构、环境诊断与受控模型发现/下载计划。
- 建立外部 Agent 通用控制面、版本化模型契约、SQLite schema v8多资源所有权和独立凭据生命周期。
- 完成 OpenCode、Pi Coding Agent、OpenClaw 与 Hermes Agent专用接入、配置预览、原生确认、
  回滚和精确断开。
- OpenClaw通过Chat Completions、OpenAI Responses和Anthropic Messages三协议官方CLI验收。
- Hermes Agent 0.18.2通过官方CLI验收，并对低于64,000 Token上下文的模型故障关闭。
- 四个外部客户端与内置HAL100 Agent的进程、配置、凭据、会话和Usage身份保持相互独立。
- 全量静态检查、单元/集成测试、四套官方客户端隔离端到端、生产构建和后台快速稳定性验收通过。
