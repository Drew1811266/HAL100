# HAL100 连续开发路线图

> 当前项目处于开发初期，只维护连续可运行的内部开发版。签名、公证、安装包、自动更新、
> 正式升级和正式发布流程不在当前路线范围内，也不作为未完成迭代。易变实现事实以
> [当前开发状态](CURRENT_STATE.md)为准。

## 当前进度

| 迭代 | 状态 |
| --- | --- |
| 迭代 0：设计与工程基线 | 已完成并接受 |
| 迭代 1：项目骨架与后台核心 | 已完成 |
| 迭代 2：网关最小闭环 | 已完成 |
| 迭代 3：OpenCode端到端 | 已完成 |
| 迭代 4：托管llama.cpp与模型管理 | Alpha核心闭环已完成 |
| 迭代 5：协议与路由完整性 | 已完成 |
| 迭代 6：标准化中文桌面界面 | 已完成 |
| 迭代 7：本地 HAL100 Agent | 两条受控纵向闭环已完成 |
| 迭代 8：云端增强 Agent | 已完成：单次云端与当前内存会话双范围闭环 |
| 迭代 9：稳定性与内部测试准备 | 已完成：快速矩阵11/11，1小时后台门槛通过 |
| 迭代 10：Pi受控操作与模型生命周期 | 已完成：8工具、RPC v2、四类写操作计划与安全模型移除 |
| 迭代 11：环境诊断与受控修复 | 已完成：10工具、RPC v3、按需快照、单项修复计划与执行后复检 |
| 迭代 12：应用边界与 Agent能力架构 | 已完成：模块化单体边界、10项能力模型、Agent职责拆分与首条前端切片 |
| 迭代 13：Agent模型发现与受控下载 | 已完成：RPC v4、13项能力、搜索—检查—计划—确认下载闭环 |
| 迭代 14：外部 Agent集成基础 | 已完成：内置Runtime/外部客户端边界、四客户端注册表、OpenCode专用适配器与Pi共存回归 |
| 迭代 15：外部 Agent通用控制面 | 已完成：模型契约、资源所有权、一次性计划、命令边界与OpenCode可逆断开 |
| 迭代 16：Pi Coding Agent接入 | 已完成：独立检测、配置、凭据、回滚、断开、桌面控制与官方CLI验收 |
| 迭代 17：OpenClaw多协议接入 | 已完成：官方配置工具、独立Secret、三协议切换、回滚、断开与真实CLI验收 |
| 迭代 18：Hermes Agent接入与共存收口 | 已完成：default Profile、YAML/.env隔离、64K门槛、真实CLI及四客户端共存验收 |
| 迭代 19：Agent能力扩展 I | 已完成：RPC v5、四客户端诊断/配置/断开事务与原生确认闭环 |
| 迭代 20：受控运维与诊断基础 | 已完成：RPC v6、四客户端全面诊断、脱敏运维历史与通用配置修复 |
| 迭代 21：部署就绪与短时观测 | 已完成：RPC v7、固定三次运行采样、部署就绪摘要与Agent快捷任务 |
| 迭代 22：版本化受管部署配方 | 已完成：RPC v8、Pi私有安装计划、固定包/SRI、原生确认、原子启用与共存优先级 |
| 迭代 23：受管依赖闭包与卸载生命周期 | 已完成：RPC v9、136包闭包指纹、私有cwd、可恢复卸载与用户Pi保护 |
| 迭代 24：信息架构与展示基础 | 已完成：五入口、稳定子路由、展示状态模型、精简首页与设置完成态 |
| 迭代 25：模型与运行任务流 | 已完成：模型添加抽屉、运行前置条件、推理服务与模型测试任务流 |
| 迭代 26：软件接入与 Agent | 已完成：五类接入摘要、Agent任务库、回答与受控计划前置 |
| 迭代 27：活动、性能与收口 | 已完成：路由分包、活动页职责、无障碍抽屉与真实Tauri恢复路径 |
| 迭代 28：界面细节与状态表达收口 | 已完成：首页就绪度、空状态任务入口、完整WebView抽屉与细节收口 |
| 迭代 29：用量可视化职责分离 | 已完成：四档Token趋势、365天活动图、schema v9与独立日/小时聚合 |
| 迭代 30：用量统计统一口径与交互收口 | 已完成：统一范围查询、筛选、计量覆盖率、构成和明细口径 |
| 迭代 31：Agent任务契约与评测基线 | 已完成：Core任务契约、18类工作流、状态机、24个配置场景和L1基线已落地 |
| 迭代 32：结构化意图与工作流入口 | 已完成：四态影子路由20/20、意图schema v1和Rust提案校验 |
| 迭代 33：Pi结构化提案与双路裁决 | 已完成：RPC v10、按需零工具提案、双重校验和8/8裁决合同 |
| 迭代 34：Pi提案质量与影子观测 | 已完成：18/18真实Qwen意图验收、确定性采样、128 Token和脱敏内存指标 |
| 迭代 35：结构化任务路由受控接管 | 已完成：Rust工作流派生工具、13/13接管合同、固定守卫与安全回退 |
| 迭代 36：任务状态机与脱敏检查点接入 | 已完成：真实生命周期、v5合同10/10、同进程精确恢复与重启失效 |
| 迭代 37：成功谓词与有界证据复验 | 已完成：18/18谓词、schema v2证据检查点、6/6故障注入和一次有界重规划 |
| 迭代 38：有界澄清与同任务继续 | 已完成：16K容量、schema v3、10/10有界澄清合同与真实Pi继续验收 |
| 迭代 39：中文开放输入、冲突与对抗评测扩展 | 已完成：41场景、18/18任务覆盖、真实Pi 24/24与失败分类 |
| 迭代 40：全部现有配置动作纵向验收 | 已完成：18/18路径、10/10任务、9/9动作与8/8失败类别 |
| 迭代 41：Agent效率基线与上下文装配 | 已完成：RPC v11、-32.7%回合、重复结果归零、16K受管配置与完整门禁 |
| 迭代 42：复合配置任务图与可恢复编排 | 已完成：真实Pi 0/0/2回合整图、现实完成证据、显式补偿与非授权恢复 |
| 迭代 43：确定性适配器扩展与设备感知上下文 | 已完成：模型停止纵向、16K/32K设备档、20轮真实连续任务与完整门禁 |
| 迭代 44：全局界面层级与排版收口 | 已完成：标题/子导航顺序、页面宽度、卡片与按钮统一，Agent重排及880/1200/1440 px视觉回归 |
| 迭代 45：首页与Agent关键任务层级校准 | 已完成：首页单卡行动结构、Agent常用窗口双栏、主动作前置及复合任务降噪 |

长期“Pi配置Agent深度融合与质量收口”目标已完成；当前尚未定义迭代46，后续阶段需重新共同规划。

迭代37已把18类成功谓词接入类型化工具观察、幂等完成和动作专属复验。schema v2检查点只保存
枚举化证据来源、三态结论和有界计数；v6合同覆盖18/18工作流与6/6故障场景。动作首次不满足
最多进入1次同任务重规划，证据缺失或第二次不满足故障关闭。真实Qwen继续通过只读完成和
非执行卸载计划验收，临时模型索引确认执行证明模型库复验后才完成。迭代38又完成16K上下文和
进程内有界澄清：v7为10/10，真实Pi从固定OpenCode选择继续到唯一配置计划，仍不持久化对话或
恢复旧授权。迭代39已完成中文开放输入、冲突和对抗评测，并以可复现失败分类约束路由和提示
改动；迭代40已用组合式证据图完成全部现有配置动作纵向验收。迭代41已完成RPC v11数值指标、
任务级指令/直接依赖装配、最终计划确定性收口、`managed-route-v2`长上下文配置以及完整质量
门禁。迭代42开始把多个受控配置目标组织为Rust权威、可验证、可中断恢复的复合任务图。
签名、公证、安装包、自动更新或正式升级流程仍不属于当前路线。

## 1. 迭代规则

- 始终维护同一个可运行开发版。
- 不在每个迭代结束后制作安装包。
- 当前不规划签名、公证、安装包、自动更新、正式升级或正式发布流程。
- 每个迭代交付一条可验证的纵向能力，而不是大量未连接模块。
- 性能、安全、数据库迁移和文档是完成条件。
- 当前仅在 Apple Silicon Mac开发和测试。
- Windows 10/11只保留平台接口，不在当前路线中实现。

## 2. 迭代0：设计与工程基线

- 固化产品、架构、安全、性能和路线图文档。
- 创建 MIT许可证和第三方许可证登记规则。
- 建立架构决策记录。
- 确认 Pi Agent Core集成边界、Sidecar安全模型和技术验证门槛。
- 以 Claude Desktop的安静工作台体验为参考，完成 HAL100自有 UI/UX规范和首版全部页面低保真线框。
- 确认信息架构、关键流程、亮暗主题方向、统一确认和 Agent工具卡交互。
- 用户确认设计基线后才进入功能代码。
- 固化工程工具链、Workspace结构、进程通信和测试质量门槛。

完成条件：文档与已确认决策一致，不存在影响项目骨架的未决产品边界；信息架构、低保真线框和单文件交互原型已经用户评审并接受。

## 3. 迭代1：项目骨架与后台核心

已完成：工程 Workspace、最小 Tauri/React壳、Core/Infra/Platform/Protocol边界、SQLite首个迁移、Gateway健康路由边界、Agent RPC v1帧与双语言测试、单实例窗口恢复路径、系统托盘、明确退出、异步结构化脱敏日志、5 MiB大小轮转、重复错误聚合及后台性能基准。Pi Agent Core已先完成“加载内核、零工具、零模型调用”冒烟验证，再完成一个真实 Rust↔Sidecar模拟工具闭环；Sidecar只提出请求，Rust重新校验白名单、精确参数和关联 ID，进程退出后无遗留资源。Sidecar启动器已落实路径约束、固定工作目录、最小环境和独立会话目录；macOS开发沙箱验证可拒绝外部文件读写、网络和任意子进程。主 WebView采用隐藏复用，100轮生产生命周期回归无资源增长；强制销毁模式保留为诊断钩子。

- Tauri 2、React、TypeScript、Vite和 Rust Workspace。
- `hal100-core`领域库和平台接口。
- SQLite迁移框架。
- 单实例、托盘、无可见窗口后台运行。
- 主 WebView按需创建，创建后隐藏复用；临时窗口按需销毁。
- 结构化脱敏日志。
- 初始空闲性能基准。
- 建立不进入产品主流程的 Pi Agent Core Sidecar技术验证：固定版本、最小私有 RPC、禁用内置工具与资源发现、评估平台权限收缩、测量冷启动和内存。

完成条件已满足：开发版可重复启动，关闭窗口后后台核心保持运行且达到初始资源预算；Pi验证只暴露 HAL100模拟工具，Sidecar退出后无遗留进程或资源。

## 4. 迭代2：网关最小闭环

已完成：桌面后台默认绑定`127.0.0.1:10100`；健康检查公开，模型与Chat入口使用独立本地Key摘要认证；开发版可连接一个外部OpenAI兼容后端。非流式和SSE均透明转发，流式路径不缓存完整回答，客户端断开会取消上游。后端Usage按客户端归属，经专用单写入线程只写一条SQLite事件。真实回环模拟后端测试覆盖Models、非流式、SSE、取消和20并发；本机p95附加延迟样本为246 μs，后台资源仍满足预算。

- 回环地址网关和健康检查。
- 本地客户端凭据。
- 外部 OpenAI兼容后端。
- `/v1/models` 和 `/v1/chat/completions`。
- SSE真流式、背压、取消和 Usage。
- 单写入 Usage事件。
- 模拟后端契约测试。

完成条件已满足：测试客户端经HAL100完成非流式和SSE推理并记录准确用量；无Key、无后端和取消路径均故障关闭或留下明确状态。

## 5. 迭代3：OpenCode端到端

已完成：检测、JSON/JSONC保留式补丁、项目配置只读诊断、语义预览、一次性确认、时间戳备份、原子替换、验证/回滚、`0600`独立Key、SQLite所有权记录、Gateway凭据热更新、工具调用透明转发和`opencode` Usage归属。所有写入测试均限定在随机临时目录。官方OpenCode CLI 1.18.11在隔离临时HOME中成功读取HAL100受管Provider和文件凭据，选择`hal100/hal100-active`，经真实Gateway SSE调用模拟后端，携带工具定义并记录精确Usage；测试未修改真实OpenCode配置。

- 检测 OpenCode及配置版本。
- 预览、确认、备份、补丁、验证和回滚。
- HAL100 Provider和 OpenCode专属凭据。
- 模型列表、Tool Calling、流式和取消验证。
- Usage正确归类为 OpenCode。

完成条件：OpenCode可通过 HAL100调用外部测试后端，现有配置不被破坏。

完成条件已满足。

## 6. 迭代4：托管 llama.cpp与模型管理

Alpha核心已完成：按需 Apple Silicon硬件检测、保守适配建议、模型存储空间检测、SQLite schema v5模型/位置/下载/审计边界、可空默认下载源设置，以及接入真实 Tauri命令的中文模型库页面。外部 GGUF可经 Rust原生选择器、固定头与完整哈希检查、语义预览、一次性计划和原生确认加入索引；源文件不会被复制、移动或删除。Hugging Face和ModelScope远程目录已统一，公开 GGUF可在确认后断点下载、取消、恢复、校验并原子安装；确认界面会区分目录服务与实际模型发布仓库。固定官方 llama.cpp Apple Silicon构建支持归档/二进制双哈希、原生确认安装/卸载、模型启动/停止/切换、模型启动前哈希、随机认证后端端口和 Gateway动态接管；内置单轮测试会产生精确 Usage并显示在统计页。Gateway流式生命周期、活动流、总字节、模型标识与SSE解析均已加固。空闲时没有目录、状态或统计轮询。

2026-08-18已用 Qwen3.5-2B Q4_K_M完成 Apple M1/16 GiB真实闭环：生产下载器安装并独立复核1,280,835,840字节 GGUF，官方 llama.cpp `b10218`通过固定供应链安装，Gateway返回中文正文并把`35`输入、`19`输出、`54`总 Token以`exact_backend_response`写入 SQLite。实测同时发现并修复混合推理模型耗尽 reasoning预算而返回空正文的问题；标准托管运行时现显式使用`--reasoning off`。

- Apple Silicon硬件检测。（已完成）
- 托管引擎供应链校验、安装/卸载确认和进程状态机。（已完成 Alpha）
- GGUF本地导入。（已完成外部只索引）
- Hugging Face和ModelScope下载适配器。（已完成公开仓库）
- 默认下载源设置。（已完成）
- 断点续传、校验、取消、重启暂停恢复和磁盘空间检查。（已完成）
- 模型启动、停止、切换、健康检查和进程退出回收。（已完成 Alpha）
- Gateway动态路由、内置模型测试与精确 Usage统计。（已完成 Alpha）
- 15分钟自动空闲卸载暂不启用；Alpha使用显式停止，避免额外后台定时唤醒。

完成条件：用户可下载或导入一个模型，经托管 llama.cpp通过网关完成推理。

完成条件已由真实下载/恢复测试、假引擎生命周期测试、Gateway Usage端到端及官方 OpenCode CLI隔离验收覆盖。外部后端发现和连接移入迭代5，不阻塞托管纵向闭环。

## 7. 迭代5：协议与路由完整性

当前协议与路由核心子阶段已完成：Chat Completions、OpenAI Responses和Anthropic Messages复用同一套有界数据面；Responses与Messages均覆盖非流式、SSE、工具结构透明转发和精确Usage。Anthropic客户端可使用`x-api-key`，HAL100会把本地客户端凭据与后端凭据隔离，并只转发协议版本与Beta能力标头。SQLite schema v6区分非流式`exact_backend_response`与流式`exact_backend_event`，从v5迁移不会丢失既有记录。

模型别名可把三个推理协议的请求路由到不同后端并重写实际模型ID，Usage同时保留请求模型、实际模型和后端归属。schema v7持久化非敏感后端元数据与别名；API Key只存macOS Keychain，启动时缺少凭据会故障关闭该后端。中文推理后端页已经支持添加、编辑、设为活动和删除外部后端，以及新增、查看和确认删除模型别名。切换活动路由会阻止新请求进入旧后端，等待包括`/v1/models`在内的已有请求排空后再原子替换；超时保持旧路由，托管llama.cpp激活失败时同时回收新进程。

故障策略也已完成首版：每个后端独立统计基础设施故障，连续3次后熔断15秒，恢复由下一次请求惰性触发，不运行后台计时器。只有幂等的Models和显式诊断请求最多重试1次；Chat、Responses和Messages推理POST从不自动重放。后端页可按需诊断已配置服务，也可检查Ollama、vLLM和llama.cpp Server的3个固定`127.0.0.1`常用地址；探测不扫描局域网，具有1秒超时和1 MiB响应上限。

强制切换闭环已经完成：Rust为每个后端维护可取消的请求代次，强制替换时只取消旧代，随后立即开放新路由；新请求不会继承旧代取消状态。非流式请求、流式响应与Models/诊断均感知取消。被中断推理只写一条失败Usage，错误类别为`forced_route_switch`。外部后端强制激活、托管llama.cpp强制切换与强制停止都必须先经过Rust发起的原生确认；普通操作继续默认使用安全排空。

- OpenAI Responses。（已完成透明协议入口）
- Anthropic Messages。（已完成透明协议入口）
- Tool Calling和缓存 Usage。（已完成三协议契约基线）
- 模型别名、`hal100-active` 和多后端路由。（核心闭环已完成）
- 请求排空和原子切换。（安全切换与原生确认强制切换均已完成）
- 错误映射、超时、有限重试和熔断。（已完成首版）
- 局域网和自定义后端。（手动配置与Keychain凭据已完成）
- 外部 Ollama、vLLM和 llama.cpp Server发现与连接。（固定回环按需发现与连接诊断已完成）

完成条件：协议契约测试覆盖流式、取消、工具和主要错误路径。

完成条件已满足。协议、路由与故障策略已由Rust协议单元测试、双后端真实回环HTTP测试、重启恢复、凭据缺失故障关闭、请求排空回滚、强制取消代次、熔断惰性恢复、幂等重试/推理不重放、固定回环发现、数据库迁移及React交互测试覆盖。最终加入请求代次取消后，本机Gateway附加p95为306微秒，仍显著低于5毫秒预算。迭代5验收记录见[强制切换闭环](benchmarks/2026-08-18-iteration-5-force-switch.md)。

## 8. 迭代6：标准化中文桌面界面

已完成：五步首次启动向导会保存当前步骤并可恢复，下载源必须显式选择，随系统登录启动首次询问且默认关闭；OpenCode之外可为每个OpenAI/Anthropic客户端签发独立Key，明文只显示一次，SQLite仅保存摘要。设置页接入真实登录项状态、固定窗口关闭行为、Token/审计保留策略和显式清理预览；审计页支持类型筛选、目标搜索和安全详情抽屉。删除过期数据与撤销客户端均由Rust发起原生确认。所有页面统一真实加载、空、错和禁用状态；统计、审计、设置和硬件不轮询，下载进度只在存在活动任务且窗口可见并聚焦时以500毫秒刷新。

2026-08-19界面迭代已将上述五步向导迁移并重构为设置页“初始化配置中心”：首次进入总览并显示轻量设置提醒，不再强制跳转；只有下载源与登录启动选择属于基础必选项，设备检测、引擎/模型和软件接入压缩为可独立处理的准备度摘要。随后完成第二轮信息密度校准，总览移除开发里程碑，硬件画像、Gateway路由、通用客户端配置和Agent边界说明改为按需展开。原有后端持久化字段继续兼容，但前端不再推进线性步骤。

同日完成第三轮排版与可视化迭代：推理后端改为非对称双栏工作区并折叠强制操作；Token统计加入无依赖静态SVG趋势图、构成环图和客户端占比，长请求表默认折叠。图表最多处理当前查询中的30个请求点，不引入后台刷新或动画。

随后完成字体统一：页面标题、区块标题、指标数字和对话框移除Georgia/宋体混排，统一使用macOS与Windows系统无衬线字体回退链；代码、地址和模型ID保留系统等宽字体。

同日完成Agent页面布局校准：三种推理范围收敛为分段选择与单条动态说明，常用任务和低频计划模板分层展示，输入与运行控制形成独立编辑器；结果区加入无动画三阶段提示和结构化空状态。宽屏使用非对称双栏，低于1100 px降为单列，不改变Pi/Rust权限边界和执行确认链。

- 按已确认的 UI/UX规范和高保真设计实现，不在此阶段重新发明信息架构。
- 首次启动向导。
- 总览、模型库、推理后端。
- OpenCode和通用接入。
- Token统计和数据保留设置。
- 测试对话。
- 设置、审计和托盘操作。
- 统一空状态、加载、错误和确认交互。

完成条件：核心流程可以不借助命令行完成；界面隐藏后前端不存在轮询、动画或周期刷新。

完成条件已满足。核心流程不借助命令行即可完成；1280×800真实浏览器预览覆盖总览、接入、审计、设置和完整首次启动向导，控制台无警告或错误。前端、Sidecar与Rust全量检查及生产构建通过；后台开发进程5次CPU采样均为0.0%，物理占用40.8 MiB、峰值43.3 MiB。验收记录见[迭代6标准化桌面界面](benchmarks/2026-08-18-iteration-6-desktop-ui.md)。

## 9. 迭代7：本地 HAL100 Agent

迭代7的两条产品纵向闭环均已完成：Pi Agent Core与 Pi AI精确固定在0.84.2，Sidecar使用 HAL100 RPC v1和单任务生命周期；Qwen3.5-2B Q4_K_M作为独立按需运行时，经内部保留别名和短期Key访问 Gateway，不改变用户活动后端。Agent可以调用Rust硬件摘要和脱敏运行目录工具，并可在先读取目录后，用精确modelId请求一个5分钟有效、只保留最新且一次消费的模型启动/切换计划。计划生成不会操作用户模型；只有Rust原生确认后才复用确定性管理器执行。活动任务支持取消，真实验收中冷启动取消6 ms、推理中取消及回收98 ms。通用聊天在模型启动前拒绝；Sidecar完成任务立即退出，模型运行时空闲2分钟退出。固定9项本机验收达到9/9，冷请求24.098秒；后台桌面私有物理占用41.2 MiB、峰值43.1 MiB。安装、卸载、删除、下载、配置写入和强制切换没有Agent执行工具，不能绕过现有Rust原生确认链。验收记录见[首条本地Agent闭环](benchmarks/2026-08-18-iteration-7-local-agent.md)与[状态、计划和取消闭环](benchmarks/2026-08-18-iteration-7-agent-actions.md)。

- 从迭代1开始维护候选模型基准。（Qwen3.5-2B本机基线已完成）
- 对比工具选择、参数、中文、冷启动和内存。（首组5项结构化基准已完成）
- 产品化 Agent Kernel Sidecar：Pi Agent Core、Pi AI、HAL100适配层和版本化私有 RPC。（已完成首版）
- 集成独立本地 Agent Model Runtime，并通过 HAL100 Gateway向 Sidecar提供模型能力。（已完成）
- 固定 Pi精确版本和依赖锁，登记 MIT版权与第三方依赖。（已完成Agent直接依赖登记）
- 禁用 Pi Coding Tools、TUI、扩展、Skills、Prompt Templates及 `.pi`/`AGENTS.md`自动发现。（已完成）
- 白名单工具、Policy Engine和一次性确认计划。（硬件/运行状态读取及模型启动/安全切换计划已完成）
- 硬件检测、模型运行目录、启动/切换计划和诊断工具。（已完成首版；安装、卸载、删除不在本迭代开放）
- 审计、取消、Sidecar崩溃处理和恢复。（已完成）

完成条件：危险操作不能绕过 Rust确认；基准场景的工具调用达到预先定义的正确率；Sidecar按任务退出、模型运行时空闲2分钟退出，且不影响 Gateway和桌面管理功能。

当前两条纵向闭环的完成条件已满足。真实Qwen固定结构化场景9/9通过；状态读取和计划生成不改变用户模型，执行必须经过Rust原生确认；取消、超时和协议异常均故障关闭并回收资源。安装、卸载和删除继续由现有桌面原生确认流程独占，未来开放对应Agent计划前必须单独评审文件所有权、供应链和二次确认边界。

## 10. 迭代8：云端增强 Agent

- OpenAI兼容和 Anthropic兼容 Provider。（已完成，复用推理后端配置）
- macOS Keychain。（已复用现有外部后端Keychain存储与启动恢复路径）
- 本地、单次云端和云端会话。（已完成；本地始终是新会话与应用重启后的默认）
- 脱敏和发送预览。（已完成Rust生成预览与原生二次确认）
- 云端 Usage独立统计。（已完成`hal100-agent-cloud`归类）
- 云端工具调用仍经过本地 Policy Engine。（已完成，与本地使用同一Tool Broker）

两条纵向闭环都使用临时内部模型别名把每项任务路由到用户明确选择的已加载后端；Sidecar只得到短生命周期本机Gateway Key，不得到云端API Key。OpenAI与Anthropic协议各自固定到`/v1/chat/completions`和`/v1/messages`，云端失败直接返回，不启动或回退本地Qwen。当前会话授权只保存`backendId + model`及固定错误码到Rust内存，不保存提示词或回答、不写SQLite、不跨应用重启；启用、退出和任务启动由同一运行锁串行化。后端删除、失载或认证失败时会话保持可见但故障关闭，用户仍可明确退出。每项任务的临时路由、Sidecar会话目录和本地客户端Key均由RAII回收；无网真实Gateway模拟验收同时证明本地Agent模型运行态不变化、Usage独立归类且上游凭据只由Gateway注入。

完成条件：未主动启用时无云端请求；密钥不进入前端、数据库和日志。

迭代8完成条件已满足。单次与当前会话模式都要求Rust生成脱敏预览；单次任务逐次确认，会话模式在启用范围时确认并持续显示状态。两者均不扩大工具权限，也不存在静默本地回退。

## 11. 迭代9：稳定性与内部测试准备

已完成：建立可重复的快速矩阵、1小时后台监控、进程暂停/恢复探针和内部测试流程。最终快速矩阵11/11阶段通过，覆盖全量检查、连续开发版生产构建、SQLite占锁恢复、100万Usage、1万模型快照、真实Sidecar生命周期与异常RPC、Gateway p95以及OpenCode 1.18.11/1.17.9。主窗口隐藏后的3,601秒观察共117个样本，平均CPU 0.0043%、最大0.3%，物理内存41.7→42.0 MiB、最大42.2 MiB；文件、TCP无增长，Gateway、安全扫描、Agent子进程和会话目录均零失败。真机整机睡眠保持为内部测试员主动执行的手工项，自动化使用5秒进程暂停/继续证明Gateway恢复且不遗留子进程。

- 1小时后台稳定性测试。
- 并发流式、百万 Usage、大模型目录和睡眠唤醒测试。
- 后端崩溃、断网、端口冲突、磁盘不足和数据库故障恢复。
- OpenCode多版本测试。
- 凭据、日志和 Agent安全检查。
- Pi依赖、Sidecar协议、禁用能力和供应链回归检查。
- 内存、句柄、连接和子进程泄漏检查。
- 内部测试说明和问题反馈流程。

完成条件已满足：架构性能、安全、故障恢复、规模和兼容性门槛均有自动报告；内部测试说明和S0—S3问题反馈流程已完成。继续维护同一个开发版，不制作安装包。

## 12. 迭代10：Pi受控操作与模型生命周期

已完成：Pi工具从3个扩展为8个，Agent RPC升级到v2。Rust仍是唯一执行权威；引擎安装/卸载、OpenCode配置和模型移除只能生成5分钟有效、只保留最新且一次消费的计划，之后必须经过Rust原生确认。模型移除采用所有权语义：托管文件重新校验路径、符号链接、大小和活动状态后移入系统废纸篓；外部模型只移除索引；内置Qwen Agent模型禁止移除。模型页和Agent页复用同一确定性管理器。

- 完成固定官方llama.cpp安装/卸载计划与OpenCode配置计划的Pi工具闭环。
- 完成模型移除预览、原生确认、活动模型互斥、TOCTOU防护、废纸篓和审计闭环。
- 完成Sidecar工具顺序、Rust白名单、协议关联、一次性计划废弃和失败关闭测试。
- 完成本机Qwen真实验收：Pi可提出危险意图，但Rust拒绝移除Agent自身依赖，模型文件与索引保持不变。

迭代10结束时未完成的Agent能力包括模型搜索/下载、失败下载分片清理、外部模型源文件删除、任意引擎命令和强制切换。后续迭代已经完成确定性模型搜索/下载、受控生命周期、强制切换和版本化私有Pi部署；外部模型源文件继续按所有权原则明确不删除。正式平台沙箱属于未来分发阶段，不计入当前连续开发版完成条件。

## 13. 迭代11：环境诊断与受控修复

已完成：新增Rust `EnvironmentDiagnostics`按需快照、Agent RPC v3的诊断与单项修复工具、桌面环境诊断卡片和执行后复检。诊断不轮询、不启动Agent模型、不读取原始日志、不执行完整模型哈希，也不把本地路径或凭据交给Pi。Pi只能复制同一任务报告中的精确`reportId/findingId`选择一个带`repairKind`的问题；Rust在生成计划前再次核对现实状态，所有执行继续要求原生确认。

- 确定性自动修复范围仅包括：缺失的固定llama.cpp安装计划、已安装但未配置的OpenCode配置计划、仍缺失的非内置模型失效索引清理。
- 引擎完整性失败、模型变化/校验失败、OpenCode冲突或外部修改、后端熔断只报告，不尝试风险修复。
- 修复计划仍复用既有确定性管理器、5分钟一次性能力对象、单计划限制和审计路径；Pi没有任意Shell、文件或进程能力。
- 执行成功后Rust生成新的诊断报告返回界面；复检本身失败不会把已经成功的确定性操作改判为失败，并写固定审计事件。
- React界面只在用户点击“立即诊断”时查询；Agent页面提供只读全面诊断与单项修复计划快捷任务，均无后台轮询。

完成条件已满足：双语言RPC/工具策略测试、Rust诊断与Agent服务测试、React交互测试和真实Qwen只读诊断验收覆盖该闭环；没有在验收中执行安装、卸载、删除或配置写入。

## 14. 迭代12：应用边界与 Agent能力架构

迭代12不新增用户可见功能，也不以拆分大文件或限制代码行数作为目标。本迭代为后续模型搜索/下载、模型生命周期修复和更多Agent能力建立可扩展的应用边界，并以Agent/环境诊断作为第一条参考切片。长期决策见[ADR-0013](adr/0013-modular-monolith-and-agent-capabilities.md)。

- 建立Gateway数据面、模型生命周期、推理运行时、软件接入、Agent、Usage/审计和平台/桌面七个业务上下文。（架构决策已完成）
- 明确React、Tauri适配、应用用例、领域策略、Infra和Platform的依赖方向，保留单进程、单Gateway和单SQLite模块化单体。（架构决策已完成）
- 为现有10个Agent工具建立内部能力模型，包含风险、数据范围、前置能力、计划语义和原生确认要求；未知能力故障关闭。（已完成）
- 保持Agent RPC v3兼容，通过适配器把现有逐工具字段映射到内部能力集合；模型搜索/下载进入实现时再独立评审RPC v4。（已完成）
- 分离Agent任务协调、Kernel/Sidecar RPC、能力策略、确定性操作计划和Provider会话职责，不再让单一服务直接承担所有变化轴。（已完成）
- 只为Kernel执行、环境只读信息、确定性计划和Provider会话建立少量稳定边界，不为每个Manager方法增加机械接口。（已完成）
- 让Tauri桌面层专注依赖装配、IPC适配、窗口/托盘生命周期和Rust原生确认；所有确定性操作仍由现有管理器复验并执行。（已完成）
- 以前端Agent与环境诊断为第一条业务切片，分离页面与诊断组件；桌面API继续作为共享适配入口，其他页面在后续实际修改时渐进迁移，不进行全量目录重写。（已完成）
- 使用特征测试固定现有行为，保留原生确认取消、一次性计划、伪造关联拒绝、取消回收、云端凭据隔离和无本地回退等安全语义。（已完成）
- 记录所有过渡适配层、事实状态源和后续所有者，避免新旧路径长期并存而无人负责。（已完成）

HAL12-02迁移记录：`hal100-core::agent_capability`是10项Agent能力身份、元数据和前置关系的事实状态源；Rust Tool Broker和桌面完成校验均查询该目录并对未知工具故障关闭。`agent_coordinator::AgentRunRequirements::to_rpc_v3`是当前过渡适配层，负责把内部能力集合映射为既有RPC v3布尔字段；Node Sidecar仍是v3契约消费者，不拥有能力策略。该适配层保留至未来独立的RPC版本决策。

HAL12-03迁移记录：桌面`agent_provider`组件拥有云端目标校验、后端可用性解析、内存会话状态、会话审计和每次运行的Provider身份选择；SQLite后端目录与Gateway运行路由仍是事实状态源。`AgentService`只通过兼容外观调用该组件，并继续在外层持有任务互斥锁，防止会话启停与Agent任务竞态。该组件不依赖Tauri、Node Sidecar、本地Agent模型或引擎/OpenCode/模型管理器，可独立验证重启不持久化和云端失效不回退；现有Tauri IPC与错误码保持不变。

HAL12-04迁移记录：桌面`agent_kernel`组件拥有固定Node运行时发现、Sidecar启动策略、0700临时会话目录、子进程/stdio生命周期、RPC帧边界、协议版本与关联ID校验、180秒响应超时、100毫秒取消感知、正常关闭和异常回收。应用层只能通过受限`AgentKernelChannel`交换已校验信封，不能接触子进程、原始流、启动环境或会话目录；工具授权和业务执行不进入传输组件。受管子进程守卫保证初始化中途失败或回调异常展开时也执行终止与回收。

HAL12-05迁移记录：桌面`agent_action`组件拥有单一待确认计划、精确ID、原生确认标记、有效期、禁止替换、一次性取用和丢弃状态规则。`AgentService`负责持有任务互斥锁、调用确定性Manager、记录执行审计与执行后复检；引擎、模型移除和OpenCode Manager仍是具体计划及现实状态复验的事实状态源。

HAL12-06迁移记录：桌面`agent_tools`应用组件拥有10个工具的Rust二次授权、前置步骤、按任务工具事件、脱敏只读快照、单计划限制、确定性Manager计划编排和计划注册审计。`agent_service`的Sidecar交换循环只处理RPC关联、次数上限、结果回传与任务完成，不再直接依赖数据库、Gateway、引擎、OpenCode、模型移除或诊断服务。具体Manager仍拥有现实状态复验与最终执行；没有为每个Manager方法增加机械接口。

HAL12-07迁移记录：桌面`agent_coordinator`组件拥有提示词职责门禁、提示词到能力集合的推导、RPC v3兼容适配、完成结果校验以及活动任务/取消生命周期。`AgentRunRegistry`只保存当前run ID和取消标记，精确租约析构负责清理；`AgentService`保留稳定外观、运行互斥、Provider/凭据/临时路由装配、确定性操作执行和审计错误映射。新增测试固定任务重叠拒绝、取消可见和精确清理语义。

HAL12-08迁移记录：React根路由只引用`features/agent/AgentPage`，Agent状态、Provider选择、运行与计划交互在该业务切片内演进；`EnvironmentDiagnosticsPanel`拥有诊断快照展示边界。现有查询键和`lib/desktop-api`保持兼容，后者继续作为Tauri与只读浏览器预览的共享适配入口，避免复制第二套业务逻辑。其他页面与全局样式不做一次性重写，后续在真实修改时按业务上下文渐进迁移。

完成条件已满足：现有Agent能力全部进入统一内部能力模型并可脱离Tauri、SQLite、Node和真实模型测试；Agent协调器不再直接承担全部数据库、Gateway、引擎、OpenCode、模型移除与Sidecar协议细节；Agent RPC v3、10个工具、Tauri IPC、SQLite schema v7、Gateway热路径和浏览器预览权限保持兼容。`pnpm check`、生产构建、Gateway 10项非忽略契约测试和121秒快速后台稳定性检查全部通过。验收记录见[迭代12：应用边界与Agent能力架构](benchmarks/2026-08-20-iteration-12-application-boundaries.md)。

## 15. 迭代13：Agent模型发现与受控下载

迭代13用第一条新增纵向能力验证迭代12的应用边界，不重写模型目录或下载系统。架构决策见[ADR-0014](adr/0014-agent-rpc-v4-model-download.md)。

- 能力注册表从10项扩展为13项，新增公开模型目录搜索、仓库GGUF检查和模型下载计划，明确公开目录数据范围、前置关系、只读/计划效果和原生确认要求。（已完成）
- Agent私有RPC升级到v4，以规范有序`requiredTools`集合替代逐工具布尔字段；Rust与Sidecar共同读取精确工具清单回归，未知、重复、乱序、缺前置或多个写计划请求故障关闭。（已完成）
- Agent搜索使用用户在HAL100选择的默认来源，最多返回8个公开仓库；仓库检查必须引用同任务搜索结果，最多返回12个带可信SHA-256的GGUF。（已完成）
- 下载计划必须引用同任务仓库快照中的精确文件，并复用现有`ModelDownloadManager`重新检查来源、仓库、修订、文件、哈希、空间、重复项和安全目标路径。（已完成）
- Agent和Pi只能生成一次性外层计划；底层下载计划ID不发送给Pi。原生确认后Rust再次复验空间并启动下载，取消确认、新任务、任务取消或失败同步废弃底层计划。（已完成）
- Agent页面增加模型下载任务入口、下载动作卡和下载/模型库查询刷新；浏览器预览继续禁用Agent运行、下载和所有写操作。（已完成）
- 保持Gateway数据面、SQLite schema v7、现有模型库Tauri IPC、既有10项工具安全语义和云端失败不回退兼容。（已完成）
- 同步架构、安全、RPC合同、路线图与验收记录；通过`pnpm check`、生产构建、Gateway契约测试和快速后台稳定性检查。（已完成）

完成条件：从自然语言任务推导出的下载能力必须按“搜索→仓库检查→一次性计划”顺序运行；任何来源未配置、仓库/文件不在同任务快照、私有或gated仓库、缺少SHA-256、空间不足、过期/取消/替换计划、浏览器写操作都故障关闭。模型下载只能在Rust原生确认后由既有管理器启动。

完成条件已满足：`pnpm check`、生产构建、Gateway 10项非忽略契约测试和121秒快速后台稳定性检查通过；SQLite保持schema v7，Gateway数据面和既有Tauri IPC未改变。验收记录见[迭代13：Agent模型发现与受控下载](benchmarks/2026-08-20-iteration-13-agent-model-download.md)。

## 16. 迭代13.1：Agent模型工具工程加固

迭代13.1不新增用户功能，不按文件行数实施机械拆分；它针对迭代13首条网络型Agent能力补齐协议策略、取消语义、结果边界和确定性纵向测试。

- 将RPC v4共享清单从工具名称数组扩展为版本化策略：固定当前最多4个必需工具、1个写计划、128 KiB成功结果预算，以及13项工具的顺序、效果、前置关系、原生确认和参数正反例。（已完成）
- Rust能力注册表、Tool Policy、RPC常量与TypeScript TypeBox/运行策略共同读取并验证共享清单；修复Sidecar此前只按长度接受不安全仓库和远端路径的契约漂移。（已完成）
- Agent远端目录和下载计划future每100毫秒观察Rust取消标记；取消时丢弃HTTP future，不等待15秒请求超时，并沿既有失败清理废弃计划、Sidecar和临时资源。（已完成）
- Rust在发送前、Sidecar在接收后分别验证128 KiB工具结果预算；远端展示名、许可证、修订与Agent文件路径增加业务字段边界，避免超长上游元数据先进入计划或审计。（已完成）
- 公开目录与下载错误向Agent暴露稳定、无敏感细节的可操作类别，区分来源/参数、网络、上游响应、空间不足、重复项与远端变化，不再统一折叠为目录或下载失败。（已完成）
- 目录和下载管理器提供保持同等URL、超时、重定向与响应策略的显式端点组合缝；桌面测试使用本地模拟服务完整覆盖搜索、同任务仓库/文件约束、一次性计划、原生确认和既有管理器SHA-256/GGUF安装，不依赖公网。（已完成）
- 保持Gateway数据面、SQLite schema v7、Tauri IPC、13项工具业务语义和前端结构不变；同步RPC、架构、安全、ADR与验收记录。（已完成）

4项能力仍是RPC v4当前单任务复杂度预算，而不是软件规模上限。后续合法工作流需要更多步骤时，应显式修改共享策略或升级协议；不通过拆散业务语义或限制模块代码量迁就该数字。前端其他业务上下文继续按ADR-0013在实际触达时渐进迁移。

完成条件已满足：`pnpm check`、生产构建、本地模拟纵向下载验收和121秒快速后台稳定性检查通过；取消等待测试小于1秒，工具结果在双方均有128 KiB故障关闭边界。SQLite保持schema v7，Gateway数据面和既有Tauri IPC未改变。验收记录见[迭代13.1：Agent模型工具工程加固](benchmarks/2026-08-20-iteration-13-1-agent-tool-hardening.md)。

## 17. 迭代14：外部 Agent集成基础

迭代14先解决HAL100内置 Agent使用 Pi Agent Core与用户独立安装 Pi Coding Agent的同机
共存边界，再为OpenCode、Pi Coding Agent、OpenClaw和Hermes Agent建立长期可扩展的专用
适配路径。架构决策见[ADR-0015](adr/0015-built-in-runtime-and-external-agent-integrations.md)。

- `hal100-core`建立内置Runtime与四个外部客户端的稳定身份注册表，集中拥有唯一
  Integration ID、Gateway客户端ID、凭据ID、协议能力、配置片段所有权和保留默认模型策略。
- 内置`HAL100 Agent`固定为`hal100-agent-runtime`/`hal100-agent`，外部Pi固定为
  `pi-coding-agent`；两者不能共享安装、命令、HOME、配置、会话、进程或凭据生命周期。
- 现有OpenCode实现明确命名为`OpenCodeIntegrationAdapter`，并从统一注册表读取身份和凭据
  边界；原有`OpenCodeManager`保留为兼容类型别名，IPC、SQLite schema v7、受控计划、
  备份、原子写入、验证和回滚行为不变。
- 新增只读Agent生态目录Tauri API和浏览器预览数据；软件接入页明确展示内置Runtime、
  OpenCode当前能力及三个后续专用适配器，不为未实现客户端提供配置按钮或写操作。
- Sidecar回归显式验证官方Pi配置、命令候选、`PI_CODING_AGENT_DIR`、
  `PI_CODING_AGENT_SESSION_DIR`和用户HOME均不进入内置Kernel；Node依赖回归确保完整
  `@earendil-works/pi-coding-agent`不会被嵌入。
- 本迭代不修改Pi、OpenClaw或Hermes用户配置，不安装、升级或卸载外部软件，不新增
  数据库迁移、后台轮询或通用插件系统。

完成条件：官方Pi无论先于或晚于HAL100安装都不能参与内置Runtime的模块解析、环境、配置
和进程生命周期；四个外部客户端身份与凭据互不冲突；OpenCode既有安全行为和前端交互保持
通过；全量检查、生产构建、Gateway契约和快速后台稳定性验收通过。

完成条件已满足：`pnpm check`、生产构建、Gateway 10项非忽略契约测试、双视口真实浏览器
验证和121秒快速后台稳定性检查通过。验收记录见
[迭代14：外部Agent集成基础](benchmarks/2026-08-20-iteration-14-external-agent-integration-foundation.md)。

## 18. 迭代15：外部 Agent通用控制面

迭代15不以文件行数或“统一万能适配器”为目标，而是提取所有外部Agent都必须遵守的稳定控制面。

- 协议目录分离“上游声称支持”与“HAL100真实验收通过”，避免UI把未验证协议当作可用能力。
- 建立版本化`hal100-active`模型契约，明确上下文、最大输出、输入模态、工具和推理能力；能力变化触发适配器刷新，而不是静默沿用乐观默认值。
- SQLite升级到schema v8；一个接入可以登记配置、凭据和辅助配置等多项资源，秘密资源禁止创建明文备份。
- 每个适配器拥有独立、5分钟、一次消费的配置与断开计划；配置计划之间不能互相替换或消费。
- 外部命令使用清空环境、固定PATH、超时和输出上限的受限运行器；CLI探测不继承用户API Key或任意环境。
- OpenCode补齐配置计划显式丢弃和完整断开流程：只移除`provider.hal100`，吊销专属Key，保留用户默认模型、其他Provider、注释和备份。

完成条件已满足：数据库迁移、资源约束、JSONC保留式移除、命令超时、计划隔离、OpenCode配置/断开回滚、桌面原生确认与全量既有回归通过。

## 19. 迭代16：Pi Coding Agent接入

- 专用`PiCodingAgentIntegrationAdapter`只管理官方`~/.pi/agent/models.json`中的`providers.hal100`；配置必须是严格JSON，用户默认模型、其他Provider、设置、扩展、Skills和会话不在所有权范围内。
- 仅从固定候选路径探测用户独立安装的`pi`，使用`PI_CODING_AGENT_DIR`时只提示覆盖风险，不静默写入自定义目录。
- Provider固定使用Chat Completions和`hal100-active`模型契约；API Key通过固定`/bin/cat`命令按请求读取HAL100应用数据目录中的`0600`专属文件，命令路径做Shell单引号转义，不接受用户自定义命令。
- 配置应用执行预览、原生确认、原文件摘要复验、备份、原子写入、严格解析验证、SQLite事务和Gateway凭据热更新；任一步失败回滚。
- 断开只移除`providers.hal100`并吊销`pi-coding-agent`Key；内置`hal100-agent`、OpenCode及其他外部客户端身份不受影响。
- 软件接入页提供Pi检测、配置刷新和可逆断开，明确显示它与内置Pi Agent Core的进程、HOME、会话和凭据隔离。
- 官方`@earendil-works/pi-coding-agent@0.84.2`在隔离HOME中读取真实受管`models.json`和凭据，经真实HAL100 Gateway调用SSE模拟后端，响应成功并把Usage归属为`pi-coding-agent`；`hal100-agent`归属保持为零。

完成条件已满足：Pi适配器单元测试、官方CLI隔离端到端、Rust workspace编译、桌面命令、前端类型检查和交互回归通过。验收记录见[迭代15–16：外部Agent控制面与Pi接入](benchmarks/2026-08-20-iteration-15-16-external-control-and-pi.md)。

## 20. 迭代17：OpenClaw多协议接入

- 专用`OpenClawIntegrationAdapter`只管理默认实例中的`models.providers.hal100`、
  `secrets.providers.hal100_gateway`和独立`0600`凭据文件。
- 配置通过官方`openclaw config patch`执行dry-run和应用；JSON5可以读取，官方工具可能
  标准化注释与排版，因此预览明确提示并保存原字节备份。
- 自定义HOME、STATE、CONFIG或Profile不会被猜测管理；低于`2026.7.1`的版本不自动配置。
- Chat Completions、OpenAI Responses与Anthropic Messages均经过真实官方CLI验收，用户可
  显式切换；切换只替换HAL100 Provider，不改变OpenClaw默认模型或运行中的服务。
- 配置、协议刷新与断开均使用独立短期一次性计划、原生确认、摘要复验、写后验证和失败回滚。
- Usage只归属`openclaw`，不与内置`hal100-agent`或其他外部客户端共享凭据。

完成条件已满足：6项适配器单元测试、官方`openclaw@2026.7.1-2`三协议隔离端到端、桌面
控制面、React回归和workspace编译通过。详见[OpenClaw接入](OPENCLAW_INTEGRATION.md)与
[迭代17验收](benchmarks/2026-08-20-iteration-17-openclaw.md)。

## 21. 迭代18：Hermes Agent接入与共存收口

- 专用`HermesAgentIntegrationAdapter`只管理default Profile的`providers.hal100`和
  `.env`中的`HAL100_HERMES_GATEWAY_KEY`，不修改粘性Profile、默认模型或其他变量。
- 兼容基线固定为Hermes 0.18.2；候选配置先在临时`HERMES_HOME`经官方
  `hermes -p default config show`验证，真实调用固定显式选择`custom:hal100`。
- Hermes官方要求至少64,000 Token上下文；当前模型契约不足时状态为Blocked，并在生成计划前
  故障关闭。真实验收使用65,536 Token契约，不伪造当前4K模型能力。
- YAML语义重写前备份不含Key的原文件；可能包含其他秘密的`.env`不创建持久备份，只在内存
  中保留事务回滚副本，并精确保留其他行、收紧为`0600`。
- 软件接入页完成检测、前置条件展示、语义预览、原生确认、应用、刷新和精确断开。
- 四个外部Agent均为可用状态，身份、凭据、受管片段、计划和Usage归属互相独立；内置Pi
  Agent Core继续使用`hal100-agent`私有生命周期。

完成条件已满足：9项Hermes单元测试、固定`hermes-agent==0.18.2`官方CLI隔离端到端、四客户端
注册表共存约束、16项React回归、生产构建和Rust workspace全目标检查通过。详见
[Hermes Agent接入](HERMES_AGENT_INTEGRATION.md)与
[迭代18验收](benchmarks/2026-08-20-iteration-18-hermes-and-coexistence.md)。

迭代15–18增加了真实适配能力和相应代码量，但没有采用文件行数硬上限。边界继续由稳定身份、
资源所有权、变化原因、协议契约和可逆事务定义；只有出现第二个相同变化原因时才继续提取共享
机制，不为减少行数合并无关客户端语义。

## 22. 迭代19：Agent能力扩展 I

- Agent RPC升级为v5，将OpenCode专用状态与配置工具收敛为四个外部Agent共用的检测、配置计划和断开计划；工具总数14项，单任务仍最多一个写计划。
- `integrationId`只能取注册表中的固定值，并由Rust绑定到用户提示中点名的软件；不能先检查一种软件再计划修改另一种软件。
- Sidecar只接收安装状态、版本、受管状态、协议和告警数量；二进制路径、配置路径、告警原文、凭据和底层适配器计划ID不进入模型上下文。
- 配置与断开复用各专用适配器的一次性计划、快照复验、备份、独立凭据、写后验证和失败回滚；Agent工具本身只生成外层计划。
- 所有写操作继续要求Rust原生确认。断开只移除HAL100受管片段和该客户端专属凭据，不删除用户配置或其他软件资源。
- 本轮不开放通用Shell、任意文件读写、Accessibility、Screen Recording或Apple Events；Pi Agent Core继续运行在清空环境和临时HOME的Sidecar中。

完成条件已满足：RPC v5双语言合同、14项工具策略、四客户端目标绑定、脱敏结果、一次性事务和原生确认均有自动回归；`pnpm check`、生产构建与真实桌面页面回归通过，控制台无运行错误。架构决策见[ADR-0016](adr/0016-agent-rpc-v5-external-agent-transactions.md)，验收记录见[迭代19：Agent能力扩展 I](benchmarks/2026-08-21-iteration-19-agent-capability-expansion.md)。

## 23. 迭代20：受控运维与诊断基础

- 全面环境诊断从OpenCode扩展到OpenCode、Pi Coding Agent、OpenClaw和Hermes Agent；客户端检测互相隔离，单个适配器失败形成固定故障项，不使整份报告泄露错误正文或本地路径。
- 诊断报告增加外部Agent已安装、已接入和待处理计数；四类客户端的未配置或需要刷新状态都能生成`ConfigureExternalAgent`修复项。
- 修复仍按“同任务报告→精确findingId→一次性外层计划→原生确认→专用适配器事务→重新诊断”执行；冲突、外部修改、不兼容版本和Blocked状态保持只读。
- RPC升级到v6并新增`inspect_operational_history`。工具最多返回24条近期事件，只保留固定事件类型、目标类型、时间、安全错误码、动作和原因标识；目标ID、提示词、回答、路径、配置正文和凭据不会进入模型上下文。
- 本轮建立L0观察与既有L1配置事务之间的纵向闭环，不开放通用Shell、任意文件读写、Accessibility、Screen Recording、Apple Events、root Helper或长期后台轮询。

完成条件已满足：RPC v6双语言合同与15项工具策略一致；四客户端诊断、通用修复映射、运维历史二次脱敏、额外参数、取消和陈旧计划全部故障关闭；Rust、Sidecar、前端、生产构建与真实浏览器页面回归通过，控制台无运行错误。架构决策见[ADR-0017](adr/0017-controlled-operations-observation.md)，验收记录见[迭代20：受控运维与诊断基础](benchmarks/2026-08-21-iteration-20-controlled-operations.md)。

## 24. 迭代21：部署就绪与短时观测

- RPC升级到v7并新增`observe_operational_health`；参数固定为部署目标和3次采样，不能请求任意采样数、长期监控或原始日志。
- Rust复用全面环境诊断，并在固定短窗口内采集Gateway活动路由、用户后端数量、熔断数量和引擎运行状态；等待响应任务取消。
- 结果只返回安装/就绪计数、固定故障码和聚合采样，不返回后端ID、路径、配置、凭据、提示词、回答或日志正文。
- 观测结果不产生写授权；修复继续复用诊断finding、一次性计划、原生确认和专用适配器事务。
- Agent页面增加“部署与运行检查”任务入口，并明确这是当前任务内的短时观察，不是后台监控服务。
- 本轮不开放Shell、包管理器、任意文件读写、Accessibility、Screen Recording、Apple Events、root Helper或常驻监控。

完成条件已满足：RPC v7双语言合同与16项工具策略一致；固定参数、额外字段、结果脱敏、采样稳定性和取消全部有自动回归；Rust、Sidecar、前端、生产构建与真实浏览器页面回归通过，控制台无运行错误。架构决策见[ADR-0018](adr/0018-bounded-deployment-readiness-observation.md)，验收记录见[迭代21：部署就绪与短时观测](benchmarks/2026-08-21-iteration-21-deployment-readiness.md)。

## 25. 迭代22：版本化受管部署配方

- RPC升级到v8并新增`plan_external_agent_installation`；必须先检测同一外部Agent，且每项任务仍最多生成一个写计划。
- 首条部署配方只覆盖Pi Coding Agent。OpenCode、OpenClaw与Hermes Agent仍可检测、配置和断开，但安装计划固定返回“配方不可用”，不借用Pi流程猜测安装方式。
- 用户已安装的Pi始终优先；检测到任何现有安装时拒绝受管安装。HAL100私有副本只位于应用数据目录，不修改PATH、HOME、用户配置、全局npm prefix或系统目录。
- 配方固定官方包、精确版本、Registry和顶层SRI；确认执行时禁用包脚本，先打包并核对归档完整性，再从本地归档安装到owner-only暂存目录。
- 原子启用前验证包名、版本、CLI入口和`pi --version`；失败只清理本次UUID暂存目录，成功与失败均写入不含路径的固定审计字段。
- 私有Pi与内置Pi Agent Core继续拥有独立进程、HOME、会话、配置和Usage身份。受管安装完成后仍需单独生成Gateway配置事务，不把安装确认扩大为配置写授权。
- 当时残余风险是npm传递依赖图仍由Registry按包依赖解析；迭代23已用固定lockfile v3语义闭包指纹关闭该风险。

完成条件已满足：受管部署执行器、单次计划、元数据漂移、符号链接根目录、归档SRI、原子安装、用户优先候选和Agent目标绑定均有自动回归；RPC v8双语言合同与17项工具策略一致。架构决策见[ADR-0019](adr/0019-versioned-managed-agent-deployment.md)，验收记录见[迭代22：版本化受管部署配方](benchmarks/2026-08-21-iteration-22-managed-agent-deployment.md)。

## 26. 迭代23：受管依赖闭包与卸载生命周期

- Pi 0.84.2配方固定lockfile v3的136项非根包语义闭包；版本、来源、SRI、依赖、可选依赖、Peer、平台、引擎与入口字段规范化后的SHA-256必须精确匹配。
- npm计划、元数据复验、打包和安装均使用绝对已复验身份、清空环境、npm同目录Node最小PATH与独立私有cwd，不读取HAL100仓库或用户项目配置。
- 顶层归档SHA-512和完整依赖闭包均在任何Pi代码执行前验证；闭包漂移故障关闭并只清理本次UUID暂存目录。
- RPC升级到v9并新增`plan_managed_external_agent_removal`；检测结果增加`managedInstallation`，明确区分用户Pi、HAL100私有Pi和HAL100配置接入状态。
- 私有卸载必须明确指向HAL100私有/受管运行时，且先检查同一Pi目标；含糊的“卸载官方Pi”不会产生文件操作计划，断开接入继续只处理配置和专属凭据。
- 卸载预览与执行前都验证固定路径、非符号链接、版本、入口和闭包；执行时原子隔离并移入系统废纸篓，废纸篓失败时恢复原目录。
- 用户Pi候选、`~/.pi`配置、凭据与会话完全不在卸载所有权范围；即使用户Pi与私有Pi同时存在，也只移动HAL100私有runtime。

完成条件已满足：真实官方npm/Registry隔离安装与卸载预览通过；依赖闭包重复解析指纹一致；受管生命周期单元测试覆盖闭包漂移、符号链接、用户Pi保护、计划单次消费、确认后变化和废纸篓失败恢复；RPC v9双语言合同与18项工具策略一致。架构决策见[ADR-0020](adr/0020-managed-dependency-closure-and-removal.md)，验收记录见[迭代23：受管依赖闭包与卸载生命周期](benchmarks/2026-08-21-iteration-23-managed-lifecycle.md)。

## 27. 迭代24：信息架构与展示基础

- 一级业务导航固定为首页、模型与运行、软件接入、Agent、活动；设置独立位于侧栏底部，外观入口不再重复出现。
- 模型、运行、推理服务和模型测试由`/workspace/*`稳定子路由承载；Usage和Audit由`/activity/*`页面外壳承载，底层查询和数据所有权不合并。
- 五个旧页面URL继续重定向到确定的新位置；既有外部链接、书签和自动化不会落入空白或占位页面。
- 首页通过纯展示`UiStatus`与`OverviewStatusView`统一底层状态，正常状态只显示一份摘要、一个推荐主动作和按需技术详情。
- `AppShell`、`PageHeader`、`StatusSummary`和`DetailDrawer`只拥有布局与展示职责；Rust领域管理器、计划、原生确认、凭据和审计边界不变。
- 设置完成后只显示可调整的下载、启动、外观和数据策略；首次使用时仍显示完整两项基础设置与完成闭环。

完成条件已满足：Desktop 23项、Agent Kernel 27项及Rust workspace 228项自动化通过；生产构建通过；真实浏览器覆盖全部新旧路由、1280×800与880×620无溢出、页面刷新非白屏，控制台零错误零警告。验收记录见[迭代24：信息架构与展示基础](benchmarks/2026-08-21-iteration-24-information-architecture.md)。

## 28. 迭代25：模型与运行任务流

- 模型首屏收敛为本地清单与“添加模型”；在线搜索、GGUF 导入、设备建议和下载任务进入统一抽屉。
- 运行页只突出当前可执行动作，模型测试并入抽屉；旧 `/test` 保持兼容。
- 本机发现与手动配置合并为“添加推理服务”流程；路由、端口、强制操作和低频引擎信息进入高级详情。
- 模型所有权、外部源文件保留、废纸篓、一次性计划和原生确认边界全部保持不变。

完成条件已满足：Desktop 行为测试、真实浏览器任务流、生产构建与全仓质量闸门通过。验收记录见[迭代25：模型与运行任务流](benchmarks/2026-08-21-iteration-25-workspace-task-flows.md)。

## 29. 迭代26：软件接入与 Agent

- 五类软件接入对象统一为注册表驱动摘要行，每行只保留一个推荐动作；详细状态、配置和断开进入抽屉。
- Agent 首屏统一状态摘要、两级 Provider 选择、最多三个上下文任务和完整 18 项分类任务库。
- 回答与受控计划前置，工具轨迹和技术 ID 折叠；旧隐藏模板 DOM 已删除。
- 云端发送范围、一次性计划、原生确认、私有 Pi 所有权和四类专用适配器语义均未弱化。

完成条件已满足：Desktop、Agent Kernel、Rust workspace、真实浏览器与生产构建全部通过。验收记录见[迭代26：软件接入与 Agent](benchmarks/2026-08-21-iteration-26-integrations-agent.md)。

## 30. 迭代27：活动、性能与收口

- Usage 与 Audit 共用活动外壳但保持独立查询和数据库所有权；用量首屏只保留主指标、趋势和最近客户端，操作筛选默认收起。
- 一级页面全部使用路由分包；首页不加载 Agent、活动图表、模型搜索或后端编辑页面实现。
- Drawer 补齐焦点循环、Escape 关闭和焦点恢复；深浅色、等效 125%/150% 缩放及 36 个页面/尺寸组合均无横向溢出。
- 真实 Tauri 启动、Gateway 401 故障关闭和开发前端失败提示均通过；错误前端显示恢复页，不再退化为空白 WebView。

完成条件已满足：Biome 71 文件、Desktop 23 项、Agent Kernel 27 项、Rust workspace 228 项、Clippy、生产构建、浏览器和 Tauri 回归全部通过。验收记录见[迭代27：活动、性能与无障碍收口](benchmarks/2026-08-21-iteration-27-activity-performance-a11y.md)。

## 31. 迭代28：界面细节与状态表达收口

- 首页按模型和运行时的真实就绪度给出下一步，不再把“核心健康”等同于“已经可以推理”。
- 模型、推理服务、用量和操作记录的空状态补齐直接任务入口；运行页在前置条件缺失时隐藏无效操作。
- Drawer 改为 `document.body` Portal，完整覆盖 WebView，并保留焦点循环、Escape、焦点恢复、滚动锁和主应用 `inert`。
- 软件接入网格、全局辅助文字对比度、Agent 推荐任务和设置 About 信息完成页面级细节收口。
- 推理服务的视觉与语义顺序统一为“服务列表 → 高级路由”，不再依赖 CSS `order` 交换交互顺序。
- 工程实现说明退出常驻界面，安全影响、预览限制和所有权边界仍在决策现场展示。

完成条件已满足：Desktop 25 项测试、TypeScript、Biome、生产构建以及 9 页面 880×620/1280×800 浏览器回归通过。验收记录见[迭代28：界面细节与状态表达收口](benchmarks/2026-08-21-iteration-28-interface-detail-refinement.md)。

## 32. 迭代29：用量可视化职责分离

- Token趋势支持年、月、周、天四档：年度按月、月度和周度按日、单日按小时聚合；三条折线分别表示缓存命中输入、缓存未命中输入和输出Token，并在图例显示所选范围合计。
- 活跃热力图固定显示过去365天，每格代表一个自然日，颜色只编码每日请求次数；Token仅在日期提示中作为精确补充值。
- 两张图改为上下独立的全宽卡片。点击任意日期格会选中该日并把Token趋势切换到“天”，展示当天24小时分布。
- Dashboard协议增加独立`dailyUsage`日聚合和按日期读取的`UsageHourlySummary`，两者均保留输入、缓存、输出和总Token分项；SQLite schema v9增加按开始时间索引，所有时间图表均不使用截断为50条的最近请求反推历史数据。
- 日聚合读取两年窗口，保证过去365天内任一选中日期切换到“年”时仍可获得完整自然年；页面继续不轮询。

验收记录见[迭代29：用量可视化职责分离](benchmarks/2026-08-24-iteration-29-usage-visualization.md)。

## 33. 迭代30：用量统计统一口径与交互收口

- 新增`UsageScopeQuery`统一查询协议，时间范围、客户端、模型、后端和状态筛选同时驱动摘要、客户端分布、日/小时聚合、Token构成和请求明细；年度格子明确作为同筛选条件下的365天导航索引。
- 默认定位最近一次活动所在月份；年、月、周、天成为整页范围，支持前后导航和回到筛选后的最近活跃日期。未来时间不再画成0，年度活动图中的过去365天始终保持统一、可操作的实心日期格。
- 摘要增加输入缓存命中率、成功率和计量覆盖率；请求数仍包含未返回usage的请求，Token只累计精确计量值。
- 折线图增加可切换系列、统一悬停提示、单位/粒度/时区说明，并减少零值节点；构成图改为可精确比较的水平堆叠条。
- 全部Token命名统一为输入（缓存命中）、输入（缓存未命中）和输出；明细表不再并列展示含缓存的输入总量与缓存量，避免被误当作可相加字段。
- `UsagePage`拆分为页面编排、日期与Token领域计算、趋势图和年度活动图四个模块；既有Dashboard与Hourly接口保留兼容，新页面只使用统一查询入口。

完成条件已满足：Biome 75文件、Desktop 25项、数据层122项自动化、TypeScript与Rust编译通过；1200×850真实本地界面覆盖默认月份、四档范围、未来截断、空范围、年度格子联动、筛选一致性、构成条和请求明细。验收记录见[迭代30：用量统计统一口径与交互收口](benchmarks/2026-08-24-iteration-30-usage-scope-refinement.md)。

## 34. 迭代31：Agent任务契约与评测基线

- 在`hal100-core`定义`AgentTaskSpec`、任务目标、期望状态、Provider模式和数据范围。（已完成）
- 为18类现有任务建立工作流注册表，明确检查、计划、原生确认、确定性执行、复验和总结步骤；
  每个检查点最多一个待确认写计划。（已完成）
- 建立可恢复任务状态机，允许确认暂停、计划过期后重规划和复验后继续，但终态不得重新打开。
  （已完成）
- 建立首版24项配置任务评测合同，覆盖四类外部Agent、Pi私有生命周期、歧义、越权、冲突、
  Sidecar崩溃、确认取消、计划过期、跨任务证据和云端不回退。（已完成）
- 建立确定性L1评测运行器，记录现有关键词路由20个场景中6个完全匹配、1个对抗写计划意图的
  基线结果，不启动真实模型。（已完成）
- 为下一阶段结构化任务入口定义兼容迁移边界；本迭代不升级RPC、不改变执行器和前端行为。
  （已完成）

完成条件已满足：Core任务架构可独立测试；评测清单映射到已注册任务和目标；现有Agent闭环、
RPC v9、18项工具、一次性计划和原生确认行为保持不变；完整质量闸门与生产构建通过。架构决策见
[ADR-0021](adr/0021-rust-owned-agent-task-architecture.md)，评测说明见
[Agent配置任务评测](AGENT_TASK_EVALUATION.md)。

## 35. 迭代32：结构化意图与工作流入口

- 在Core建立`Task`、`Clarify`、`Reject`和`Unresolved`四态路由结果，只携带有界语义。（已完成）
- 建立确定性结构化入口，覆盖18类注册任务以及目标缺失、所有权不明、多个写目标、Shell越权和
  用户配置删除越权。（已完成）
- 在24场景合同的20个文本场景中达到20/20，两个对抗场景均不形成任务；保留旧路由6/20基线
  作为迁移对照。（已完成）
- 定义`contracts/agent-intent/v1-schema.json`，禁止Pi提案包含工具、权限、Provider、执行参数
  或任意解释文本。（已完成）
- 建立Rust提案校验器，复验schema版本、精确字段、任务注册、目标类型和外部Agent注册ID，再
  构造`AgentTaskSpec`。（已完成）
- 在现行任务入口以影子模式计算并记录枚举化结果，不保存提示词、不改变工具选择和用户行为。
  （已完成）

完成条件已满足：18类任务入口可达；20个文本场景完全匹配；对抗任务数为0；旧关键词执行链、
RPC v9、18项工具、原生确认和确定性执行器保持不变；完整质量闸门与生产构建通过。决策见
[ADR-0022](adr/0022-shadow-intent-routing-and-pi-proposal-contract.md)，评测记录见
[迭代32结构化影子路由](benchmarks/2026-08-25-iteration-32-structured-shadow-routing.md)。

## 36. 迭代33：Pi结构化提案与双路裁决

- Agent RPC升级到v10，增加独立`agent.intent.start`与`agent.intent.completed`消息；18项工具、
  单任务预算和执行消息保持不变。（已完成）
- 只有确定性结构化入口返回`Unresolved`时才调用Pi，明确任务、澄清和拒绝保持零额外模型回合。
  （已完成）
- Pi意图调用不注册工具；Sidecar只接受意图schema v1的精确JSON并重建规范对象，不转发原始
  模型输出、Markdown、额外字段或Provider错误正文。（已完成）
- Rust复验schema、任务、目标、外部Agent ID和Provider，并建立`Agreement`、
  `DeterministicGuard`、`DeterministicOnly`、`ProposalCandidate`、`Conflict`和`Unresolved`
  六态裁决。（已完成）
- 新增8场景固定合同，覆盖按需调用、确定性安全规则优先、相同/冲突提案、无效提案和双方未解析；
  自动回归达到8/8。（已完成）
- 保持影子模式；裁决结果不改变`requiredTools`、写计划、原生确认、执行器或回答。（已完成）

完成条件已满足：Pi真实参与长尾意图提案，明确请求不增加模型开销；原始输出不跨越Sidecar；
双语言RPC、提案校验和Rust裁决回归通过；现行18项工具与确定性执行边界不变。决策见
[ADR-0023](adr/0023-on-demand-pi-intent-proposals-and-dual-adjudication.md)，验收记录见
[迭代33按需Pi意图提案](benchmarks/2026-08-25-iteration-33-pi-intent-proposal-boundary.md)。

## 37. 迭代34：Pi提案质量与影子观测

- 修正外部Agent确定性入口的过度匹配：只有明确检查语义才形成检查任务；已知目标但动作不明的
  表达返回`Unresolved`并交给Pi，不再静默误判为检查。（已完成）
- 在Core把受管安装与移除目标严格限制为`pi-coding-agent`，Sidecar系统提示同步该约束。
  （已完成）
- 意图专用模型固定`temperature=0`、最多128输出Token和零工具；通过外部Agent名称映射、动作
  同义规则和短正反例提高2B模型稳定性，不放宽精确JSON校验。（已完成）
- 新增v3真实意图合同：6个确定性入口故意未解析的长尾配置、检查、受管生命周期和安全场景，
  每项运行3次。（已完成）
- 建立进程内`AgentIntentShadowMetrics`，只聚合调用/状态/裁决计数和毫秒耗时；提示词、回答、
  run ID、目标和凭据不进入指标，应用重启后清空。（已完成）
- 显式运行真实本地Qwen3.5-2B：最终18/18结构化、18/18语义精确、6/6安全拒绝，p95和最大
  意图延迟均约2.61秒，未注册工具且未发生系统变更。（已完成）
- 保持影子模式，真实结果只证明进入受控接管阶段的前置门槛，不直接授权工具路由切换。
  （已完成）

完成条件已满足：真实模型多轮结果达到合同门槛；无提示词影子指标可由现有Agent状态读取；
确定性规则、Sidecar和Rust三层目标约束一致；现行18项工具、一次性计划、原生确认和确定性执行
保持不变。决策见[ADR-0024](adr/0024-deterministic-pi-intent-quality-observation.md)，验收记录见
[迭代34 Pi提案质量与影子观测](benchmarks/2026-08-25-iteration-34-pi-intent-quality.md)。

## 38. 迭代35：结构化任务路由受控接管

- 为18类`AgentTaskKind`建立Rust拥有的单一叶能力映射；`AgentCapabilitySet`只从该映射闭合
  前置能力，Pi提案中仍不存在工具名、参数或授权。（已完成）
- 让`Agreement`、`DeterministicOnly`和`ProposalCandidate`中的可信任务生成规范
  `requiredTools`，同时把外部Agent目标绑定到经过注册表验证的任务目标。（已完成）
- 确定性澄清与拒绝由Rust直接生成固定回答，不启动Kernel、不加载模型、不注册临时凭据，也
  不调用任何工具。（已完成）
- Pi澄清与拒绝在意图回合后固定收口；含工具的旧路由若最终仍未解析则故障关闭，只有旧能力集合
  为空的解释类请求可以进入零工具兼容路径。（已完成）
- 提供开发期`safe-legacy`回退模式：只允许确定性已识别任务使用旧能力计算，绝不激活只有Pi
  识别出的任务；默认模式为`controlled`，状态中明确返回当前模式。（已完成）
- 新增不持久化的`AgentTaskRoutingMetrics`，只聚合结构化确定性/Pi接管、守卫、兼容、回退和
  故障关闭计数，不含提示词、回答、任务类型、目标、run ID或凭据。（已完成）
- 新增v4受控路由合同，13个场景的决策和精确工具集合均为13/13；真实本地Qwen长尾Hermes检查
  只调用`inspect_external_agent`且计划为0。（已完成）

完成条件已满足：结构化任务成为工具选择的Rust事实源；Pi不能扩大能力；所有变更能力仍只生成
一次性计划并要求原生确认；安全守卫和未解析工具请求不会回落到旧执行；开发期回退不会激活Pi
独有任务。决策见[ADR-0025](adr/0025-controlled-structured-task-routing-cutover.md)，验收记录见
[迭代35结构化任务路由受控接管](benchmarks/2026-08-25-iteration-35-controlled-task-routing.md)。

## 39. 迭代36：任务状态机与脱敏检查点接入

- 新增Rust进程内`AgentTaskRuntime`，直接持有Core的`AgentTaskState`；只有经过裁决并被受控路由
  接受的任务可以进入状态机，模型和前端不能提交阶段。（已完成）
- 只读任务沿检查、复验、完成闭环；写任务沿检查、规划、等待确认暂停，精确确认后才进入执行、
  复验和完成。（已完成）
- 将一次性计划与当前任务私有关联；伪造ID不能消费或推进状态，原生确认取消、新任务、运行失败
  和主动取消同步撤销底层计划并进入终态。（已完成）
- 状态读取和计划访问会按固定截止时间回收过期计划，不增加后台轮询；应用重启后任务、计划和
  确认权限均不恢复。（已完成）
- `AgentStatus`增加schema v1脱敏检查点，只返回任务/目标类别、期望状态、Provider/数据范围、
  阶段序列、待确认、复验和恢复枚举，不含用户内容、具体资源、计划/run ID或工具结果。（已完成）
- 新增v5生命周期合同，10类状态与失效场景达到10/10；Protocol和Desktop负面测试证明检查点
  敏感字段为0。（已完成）
- 真实Qwen长尾只读任务到达`completed/satisfied`；引擎卸载任务停在等待确认，测试取消后转为
  已取消且引擎保持安装。（已完成）

完成条件已满足：Rust是任务阶段唯一事实源；同进程精确确认是唯一恢复入口；重启不恢复授权；
执行成功必须经过复验状态；Agent RPC v10、18项工具、SQLite schema v9和Gateway数据面保持
不变。决策见[ADR-0026](adr/0026-in-process-redacted-agent-task-checkpoints.md)，验收记录见
[迭代36任务状态机与脱敏检查点](benchmarks/2026-08-25-iteration-36-task-checkpoints.md)。

## 40. 迭代37：成功谓词与有界证据复验

- 把18类工作流已定义的`success_predicate`接入只读完成和执行后复验，不再仅以工具/执行器调用
  成功代表任务目标满足。（已完成）
- 为模型、引擎、外部Agent、模型目录、诊断修复和运维观察分别定义Rust拥有的有界证据摘要，
  不保存原始工具结果、配置、路径或用户内容。（已完成）
- 区分`Satisfied`、`Unsatisfied`和`EvidenceUnavailable`，只允许非终态在固定次数内返回规划；
  不允许模型自行声明完成或无限重试。（已完成）
- 建立动作专属故障注入、真实Pi纵向验收和版本化合同，保持未授权写操作为0。（已完成）
- `AgentTaskSpec`为每类任务定义固定证据来源白名单；错误工作流的证据不能推进状态。只读空目录
  明确为`Unsatisfied`，诊断无足够结论明确为`EvidenceUnavailable`。（已完成）
- 模型启动/移除、引擎安装/移除、四类外部Agent配置/断开、Pi私有安装/移除和诊断修复分别读取
  动作专属现实状态；执行器摘要、模型回答、Sidecar与前端均不能写入结论。（已完成）
- schema v2检查点和Agent技术详情显示成功谓词、证据来源、观察数及重规划计数；检查点仍不
  持久化。Agent专属动作审计只保留动作类别，不保存计划/run ID、具体目标或路径。（已完成）
- v6合同达到18/18工作流和6/6故障注入；临时模型索引真实确认执行到达
  `completed/satisfied/modelLibraryRecheck`，真实Qwen长尾只读与引擎计划验收通过。（已完成）

完成条件已满足：18类谓词全部由Rust事实触发；无证据不完成；非终态最多重规划1次；真实执行
必须经过动作专属复验；检查点、Agent证据审计和模型上下文均不新增敏感持久化。Agent RPC v10、
18项工具、SQLite schema v9和原生确认边界保持不变。决策见
[ADR-0027](adr/0027-rust-success-predicates-and-bounded-evidence.md)，验收记录见
[迭代37成功谓词与有界证据复验](benchmarks/2026-08-25-iteration-37-success-predicates.md)。

## 41. 迭代38：有界澄清与同任务继续

- 将内置Qwen/Pi上下文从6144提高到16384，建立容量合同并验证M1/16 GiB内存、延迟、取消、
  回收和真实9场景；按原提示拒绝不必要澄清，统一解释型请求的新旧零工具语义。（已完成）
- 将Core已有`Clarifying`阶段接入真实结构化任务，只为缺失的注册目标或所有权选择生成固定、
  有界且不授权的澄清请求。（已完成）
- 在同一进程内用任务类别和非敏感槽位继续澄清后的同一任务；不保存提示词、回答、具体目标、
  计划或旧确认，不在应用重启后恢复。（已完成）
- 明确澄清次数、超时、替换、取消和冲突上限；超过上限故障关闭，不能退回开放式聊天或旧工具
  路由。（已完成）
- 建立版本化合同、真实Pi长尾验收和用户可见状态，保持工具集合、原生确认和确定性执行边界。
  （已完成）

本迭代已完成。不得扩大Pi工具权限、持久化对话或引入签名、公证、安装包、自动更新和正式升级
流程。决策见[ADR-0028](adr/0028-versioned-agent-context-capacity.md)和
[ADR-0029](adr/0029-in-process-bounded-agent-clarification.md)；验收见
[迭代38 Agent上下文容量](benchmarks/2026-08-25-iteration-38-agent-context-capacity.md)与
[迭代38有界澄清](benchmarks/2026-08-25-iteration-38-bounded-clarification.md)。

## 42. 迭代39：中文开放输入、冲突与对抗评测扩展

- 建立不复用训练/模板措辞的中文开放输入集，覆盖18类任务、三类澄清、解释型零工具请求和范围外
  拒绝；按确定性入口、Pi意图、受控工具和最终状态分层评分。
- 增加口语、省略、错别字、混合中英文、目标/动作冲突、提示注入、所有权扩大和多目标写请求；
  每个真实Pi场景重复运行并记录错误类别、模型回合、工具结果Token与延迟。
- 先固化基线和失败样本，再以证据决定路由、提示装配或适配器修复；不得为提高分数扩大工具权限。

本迭代已完成。v8主集41场景覆盖18/18任务；确定性入口最终31/41、越权任务0，9个安全长尾
保留给Pi。独立真实Pi子集12场景×2轮达到24/24，安全4/4，p95为2260毫秒。剩余单目标动作
冲突已分类为后续专属澄清槽位，不错误复用目标澄清。决策见
[ADR-0030](adr/0030-open-input-agent-evaluation-and-failure-taxonomy.md)，记录见
[迭代39中文开放输入评测](benchmarks/2026-08-25-iteration-39-open-chinese-agent-evaluation.md)。

## 43. 迭代40：全部现有配置动作纵向验收

- 为每类现有配置写动作建立自然语言→任务→精确工具→一次性计划→原生确认→确定性执行→现实
  状态复验的隔离纵向场景。（18条v9类型化路径及下层执行证据绑定已完成）
- 覆盖幂等已满足、计划过期、确认取消、目标变化、执行失败、复验不满足、一次重规划和回滚；
  任何模型文字或执行器摘要都不能替代最终状态。
- 首先盘点现有动作与夹具缺口，再按风险补齐测试，不触碰签名、安装包、更新或正式升级流程。

Core已锁定10类受控任务到9种原生动作的允许关系；v9以18条路径展开四适配器配置/断开和3类
诊断修复执行器，并绑定精确工具、默认门禁内的隔离执行测试及复验证据。公开计划与私有执行器
错误配对在注册前拒绝；8类关键失败证据全部通过，合同路径最终状态证据率18/18。

本迭代已完成。决策见[ADR-0031](adr/0031-rust-owned-controlled-action-contracts.md)，验收记录见
[迭代40全部受控动作纵向证据](benchmarks/2026-08-25-iteration-40-controlled-action-verticals.md)。

## 44. 迭代41：Agent效率基线与上下文装配

- 建立不保存提示词、回答和原始工具结果的任务级效率指标，区分意图模型调用、执行模型回合、
  工具调用、工具结果字节/Token和上下文预算使用。
- 用迭代31固定场景和当前v8/v9任务集建立可重复对照，明确旧链路与结构化任务链路的平均模型
  回合及重复工具结果Token口径。
- 在不删除成功复验、原生确认和安全检查的前提下做按任务上下文装配、去重与确定性短路，目标为
  平均模型回合下降至少30%、重复工具结果Token下降至少40%。
- 真实Pi重型验收继续记录聚合指标和场景ID，不保存用户内容，不以扩大工具权限换取效率。
- HAL100托管用户模型及Pi/OpenCode/OpenClaw受管模型配置统一提升到16384 Token；Hermes的
  64000 Token最低要求继续故障关闭，不用虚报上下文换取表面兼容。

实现结果：v9的18条动作路径最小回合从55降到37（-32.7%）；三工具隔离对照重复结果Token从
非零降到0，发送结果不超过旧值60%；真实本地Qwen以16K窗口、2个执行回合、0次纠偏和0重复
结果完成模型计划。RPC v11指标由Rust复核后才进入结果和脱敏审计，工具权限、原生确认和现实
复验不变。

本迭代已完成：完整`pnpm check`、`pnpm build`与`git diff --check`均通过。决策见
[ADR-0032](adr/0032-task-scoped-agent-context-and-efficiency.md)，验收记录见
[迭代41 Agent效率与长上下文装配](benchmarks/2026-08-25-iteration-41-agent-efficiency-and-context.md)。

## 45. 迭代42：复合配置任务图与可恢复编排

- 由Rust根据用户已确认的复合目标构建有向无环任务图；Pi只负责理解意图和在既有任务合同内
  提议，不能创建未知节点、修改依赖、提交参数或扩大工具权限。
- 每个节点绑定既有`AgentTaskKind`、成功谓词、证据来源和受控动作；节点完成必须来自现实状态
  复验，模型文字、计划摘要或前序节点成功都不能替代证据。
- 明确定义`blocked`、`ready`、`running`、`awaitingConfirmation`、`succeeded`、`failed`、
  `compensating`与`cancelled`等状态，以及失败传播、幂等跳过、有限补偿和人工确认边界。
- 断点续作只恢复无授权的图结构、节点枚举状态和脱敏证据摘要；一次性计划、确认授权、run ID、
  提示词、回答、路径、凭据与原始工具结果不得持久化，重启后写节点必须重新规划和确认。
- 先以“准备模型运行环境并接入一个外部Agent”等代表性复合场景建立合同和隔离评测，再接入
  Agent页面；不得以自动批量执行绕过逐项原生确认。

完成条件：复合图合同覆盖依赖排序、幂等跳过、节点失败、取消、过期、重启恢复和有限补偿；
至少一条真实Pi复合场景能在不扩大工具权限的前提下推进到逐项确认并由现实证据完成；完整质量
门禁和文档同步通过。

当前进度：Core已实现最多8节点/4依赖的DAG、3/4节点Rust工厂、证据解锁、失败传播、取消、
显式逆序单次补偿、脱敏schema v1检查点和重启全节点重验证；v10合同记录12类语义。协议、桌面
逐节点Sidecar、一次性计划、原生确认关联、动作复验和Agent页面的创建/继续/取消均已接入。
真实Pi已在零写入条件下完成前两节点并停在模型节点确认，Rust预检将该路径模型回合从9降至3。
执行前后同一Rust谓词的`Unsatisfied→Satisfied`现在是唯一真实变更归属；隔离OpenCode场景已
完成配置、下游失败、显式逆序断开、新计划/新确认和断开复验，零自动补偿。v11又完成16KiB/
0600脱敏检查点、未知字段与错形拒绝、用户重新选择精确目标、全节点现实重验证和终态清理。
最终真实Pi隔离整图已完成：引擎与模型节点由Rust现实预检以0回合幂等完成，OpenCode节点由真实
Qwen/Pi在2回合生成精确计划；伪造plan ID拒绝，精确计划经确认路径执行并由接入状态复验完成，
重复工具结果0，整图`succeeded`，且配置、凭据、数据库和Gateway均位于临时隔离目录。迭代42
完成条件已满足，完整质量门禁通过。决策见
[ADR-0033](adr/0033-rust-owned-composite-task-graphs.md)，阶段记录见
[迭代42复合任务图基础合同](benchmarks/2026-08-25-iteration-42-composite-graph-foundation.md)与
[迭代42显式补偿纵向验收](benchmarks/2026-08-26-iteration-42-explicit-compensation.md)、
[迭代42非授权恢复纵向验收](benchmarks/2026-08-26-iteration-42-redacted-recovery.md)、
[迭代42真实Pi整图验收](benchmarks/2026-08-26-iteration-42-real-pi-composite.md)。

## 46. 迭代43：确定性适配器扩展与设备感知上下文

- 以v8开放输入失败分类、v9动作纵向和真实使用证据筛选新能力；只有存在Rust确定性读取/执行器、
  原生确认、现实成功谓词和故障关闭测试的能力才能新增任务，不能因模型能描述就扩大工具权限。
- 复核原有18类任务对配置工作流的缺口，优先补充能减少用户手工跨页面操作且不需要任意Shell、
  任意文件读写、桌面自动化、root或用户所有配置覆盖的适配器。
- 保留16384 Token为所有支持设备的已验证基线；建立设备内存、模型KV占用、首Token/总延迟、取消
  和空闲回收的版本化分档合同，只在真机证据满足预算时开放更高固定档或自适应档。
- 上下文档位由Rust设备策略决定并受合同上限约束，Pi、提示词、远端响应和用户输入都不能请求
  提高容量；工具结果压缩和任务级提示装配在所有档位保持一致。
- 新适配器、任务或上下文档位必须补合同、隔离纵向、真实Pi验收、ADR/基准和文档漂移检查。

完成条件：至少一个由缺口证据支持的新确定性能力完整通过意图→计划→确认→执行→现实复验；
16K以上档位若开放，必须在对应Apple Silicon内存档真机通过延迟、内存、取消、回收与重复任务
稳定性门槛，否则保持16K并记录拒绝证据；完整`pnpm check`、`pnpm build`与差异卫生通过。

阶段结果：Apple M1/16 GiB已用27,725 Token真实输入验证32K，未截断且停止回收通过；
`agent-runtime-v2`只允许Rust在16K基线和32K标准档之间选择，RPC v12把精确容量传给Pi并由
Sidecar故障关闭合同外数值，`managed-route-v3`同步驱动托管模型和受管外部Agent描述。64K尚无
对应最低设备证据，因此未开放。

缺口证据选择了已有Rust确定性执行器但Agent页缺失的“停止当前模型”：新增第19类任务、
第19项RPC工具、第15种成功谓词和第10种原生动作。该路径要求先读运行目录，计划与执行前均
复核当前活动`modelId`，使用原生确认并以`RuntimeRecheck`判定停止，不删除文件、索引或用量。
隔离真实Pi在32K档用2回合完成，伪造计划被拒绝且模型数据保留。v12设备稳定性合同另以7/7
选择边界覆盖未知内存、16 GiB阈值和高内存仍封顶32K；除M1/16 GiB外不冒充多机实测。真实
32K连续任务20/20、最大2执行回合、重复结果0，显式停机后活动任务和子运行时均为0。最终
`pnpm check`、`pnpm build`与`git diff --check`均通过；本迭代已完成。

## 47. 迭代44：全局界面层级与排版收口

- 以首页、模型与运行、软件接入、Agent、活动和设置的真实浏览器状态进行跨页面视觉巡检，优先
  修复层级、密度、对齐和响应式问题，不改变现有业务职责与安全边界。（已完成）
- 统一页面标题、同域子导航、内容宽度、卡片表面、按钮和间距系统；页面标题固定先于子导航，
  普通业务页使用1120 px内容上限，Agent使用1140 px上限。（已完成）
- 重排Agent状态、技术详情、复合任务图、单任务输入与结果，使宽窗口保持清晰双栏、常用窗口
  自动转为单列；复合任务动作不再形成高噪音的大色块。（已完成）
- 在880 px最小窗口检查7个现行入口的横向溢出，在1200/1440 px覆盖亮色、深色及Agent单/双栏；
  浏览器控制台错误必须为0。（已完成）

本迭代不改变Pi权限、RPC、SQLite、签名、公证、安装包、自动更新或正式升级流程。验收记录见
[迭代44全局界面层级与排版收口](benchmarks/2026-08-26-iteration-44-ui-coherence.md)。

## 48. 迭代45：首页与Agent关键任务层级校准

- 首页把状态、下一步说明、主动作、系统详情和次级入口合并为单一卡片，修复状态标签继承全局
  自动外边距后脱离标题的问题，消除同一阻塞事项的重复大型卡片。（已完成）
- Agent在1100 px以上保持任务输入与结果并视，最小窗口再安全转为单列；空结果面板使用稳定高度
  并在工作台滚动时保持可见。（已完成）
- 闲置复合任务移到常用单任务之后，只有活动、补偿或可恢复任务提升；创建表单在常用窗口恢复
  同行字段与动作，运行主动作先于推荐模板。（已完成）
- 压缩Agent状态卡的遗留三行网格、移除同层级重复胶囊，并统一复合任务下拉框和按钮高度。
  （已完成）

本迭代不改变Pi能力、权限、RPC、数据库或任何发行流程。用户截图尺寸1159 × 846、最小窗口
880 × 700和深色主题均通过Playwright回归，记录见
[迭代45首页与Agent关键任务层级校准](benchmarks/2026-08-26-iteration-45-home-agent-hierarchy.md)。

## 49. 每个迭代的统一完成条件

- 开发版能启动并保留已有完整闭环。
- Rust测试、前端测试、类型检查和静态检查通过。
- 新数据库变更具有向前迁移和失败处理。
- 不提交 API Key、模型权重和非必要大二进制。
- 默认不保存提示词和回答。
- 新系统能力经过安全边界评审。
- 性能基准无未解释的显著退化。
- 文档与实现同步更新。
