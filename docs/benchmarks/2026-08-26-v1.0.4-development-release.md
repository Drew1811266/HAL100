# HAL100 1.0.4 开发版收口验收

- 日期：2026-08-26
- 平台：Apple Silicon macOS
- 版本：1.0.4
- 结论：通过

## 版本边界

- 本版本是早期开发进度标签，不包含签名、公证、安装包、自动更新或正式升级流程。
- Root package、Desktop package、Agent Kernel Sidecar、Tauri 配置、Rust workspace 与 Cargo lock 中的 HAL100 自有包统一为 `1.0.4`。
- `v1.0.3` 保留为历史标签；1.0.4 使用新的提交与 `v1.0.4` 标签，不改写旧版本。

## 开发进度

- 迭代 31—45 将 Pi 内核收敛为 Rust 掌权的配置 Agent，覆盖 19 类任务、19 类成功谓词、受控动作纵向、有界澄清、按需意图提案和任务级上下文装配。
- RPC v12、固定复合任务图、显式补偿、脱敏检查点恢复和设备感知 16K/32K 上下文策略已经落地；64K 因缺少最低设备实测继续关闭。
- OpenCode、Pi Coding Agent、OpenClaw 与 Hermes 保持独立生命周期，并通过 Rust 一次性计划、原生确认、确定性执行和现实复验完成受控配置。
- 首页与 Agent 页完成第二轮信息层级校准，常用窗口保持任务输入与结果并视，空闲复合图降级，执行动作前置。

## 质量闸门

- `pnpm check`：文档一致性、Biome、TypeScript、Desktop 25 项测试、Agent Kernel 34 项测试、Cargo fmt、Clippy `-D warnings` 与 Rust workspace 测试通过。
- `pnpm build`：Desktop、Agent Kernel Sidecar 与 Rust workspace 开发构建通过。
- 1159×846、880×846 与深色模式视觉回归无横向溢出，浏览器控制台错误为 0。
- `git diff --check`、版本一致性检查与远端同步检查通过。
